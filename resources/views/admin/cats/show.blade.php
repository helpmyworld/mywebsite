@extends('admin.layout.admin')@section('content')    <div class="col-md-10 col-md-offset-2">        <div class="col-md-4 compose">            <div class="mail-profile">                <div class="mailer-name">                    <h3>Category | <strong>{{$category->name}}</strong></h3>                </div>                <div class="clearfix"> </div>            </div>            <div class="compose-bottom">                <br>                <span><a href="{{route('categories.edit', $category->id)}}"                         class="btn btn-primary btn-block">Edit Category</a></span>                    {{ Form::open(['route' => ['categories.destroy', $category->id], 'method' => 'DELETE']) }}                    {{ Form::submit('Delete', ['class' => 'btn btn-danger btn-block', 'style' => 'margin-top:20px;']) }}                    {{ Form::close() }}            </div>        </div>        <div class="row">            <div class="col-md-6">                <h2><b>{{$category->name}}</b><small> Is In <strong style="color:red;">                            {{$category->posts()->count()}}</strong>Posts</small></h2>                <table class="table">                    <thead>                    <tr>                        <th>Post Name</th>                        <th>Links</th>                    </tr>                    </thead>                    <tbody>                    @foreach($category->posts as $post)                        <tr>                            <th>{{$post->title}}</th>                            <th><a href="{{route('blog.single', $post->slug)}}" class="btn btn-info btn-lg">Go To Post</a></th>                        </tr>                    @endforeach                    </tbody>                </table>            </div>        </div>    </div>@stop